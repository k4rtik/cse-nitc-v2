This was an attempt to get the markup and structure of the website right before diving into the color scheme and other visual effects.

This looks more like a base template for building your own theme by suitably customizing the layout and styles using CSS.

Done during December of 2009, this attempt was abondoned after deciding on a dynamic website instead of a plain static one.
